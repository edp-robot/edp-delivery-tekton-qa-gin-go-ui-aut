apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gin-go-ui-aut.fullname" . }}-test-connection"
  labels:
    {{- include "gin-go-ui-aut.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gin-go-ui-aut.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
